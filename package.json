{
  "name": "easyq-crm",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:worker": "npm run build && wrangler dev",
    "dev:worker:remote": "npm run build && wrangler dev --remote",
    "db:init:local": "wrangler d1 execute easyqueue_db --local --file=../easyqueue-business-bot/schema.sql",
    "db:migrate:local": "wrangler d1 execute easyqueue_db --local --file=../easyqueue-business-bot/migrations/2026-04-06-booking-slots.sql",
    "db:migrate:local:payments": "wrangler d1 execute easyqueue_db --local --file=../easyqueue-business-bot/migrations/2026-04-06-payments.sql",
    "db:migrate:local:crm": "wrangler d1 execute easyqueue_db --local --file=./migrations/2026-07-28-crm-owned-tables.sql",
    "db:migrate:local:booking-phone": "wrangler d1 execute easyqueue_db --local --file=./migrations/2026-07-28-booking-client-phone.sql",
    "db:migrate:remote:booking-phone": "wrangler d1 execute easyqueue_db --remote --file=./migrations/2026-07-28-booking-client-phone.sql",
    "db:migrate:local:staff-role": "wrangler d1 execute easyqueue_db --local --file=./migrations/2026-07-28-staff-role-phone.sql",
    "db:migrate:remote:staff-role": "wrangler d1 execute easyqueue_db --remote --file=./migrations/2026-07-28-staff-role-phone.sql",
    "db:migrate:local:staff-access": "wrangler d1 execute easyqueue_db --local --file=./migrations/2026-07-28-staff-access.sql",
    "db:migrate:remote:staff-access": "wrangler d1 execute easyqueue_db --remote --file=./migrations/2026-07-28-staff-access.sql",
    "db:migrate:local:session-version": "wrangler d1 execute easyqueue_db --local --file=./migrations/2026-07-28-session-version.sql",
    "db:migrate:remote:session-version": "wrangler d1 execute easyqueue_db --remote --file=./migrations/2026-07-28-session-version.sql",
    "build": "vite build",
    "typecheck": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260320.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.4.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "wrangler": "^4.79.0"
  }
}
